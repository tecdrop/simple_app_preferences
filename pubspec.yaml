name: simple_app_preferences
description: A simple, type-safe, and easy-to-use app preferences package for Flutter.
repository: https://github.com/TechAurelian/simple_app_preferences
issue_tracker: https://github.com/TechAurelian/simple_app_preferences/issues
version: 0.0.2

environment:
  sdk: '>=2.19.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
