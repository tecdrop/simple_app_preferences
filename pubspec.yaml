name: simple_app_preferences
description: A simple, type-safe, and easy-to-use app preferences package for Flutter.
repository: https://github.com/tecdrop/simple_app_preferences
issue_tracker: https://github.com/tecdrop/simple_app_preferences/issues
version: 0.0.4

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# The following section is specific to Flutter packages.
flutter:
